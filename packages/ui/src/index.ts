// @repo/ui — shadcn/ui components (shared, themable).
// Module 07 runs `shadcn@latest init` against this package (monorepo
// template) and switches to granular per-component exports so importing
// Button doesn't pull in TanStack Table. See plan.md Part D, Module 07.
export {};
