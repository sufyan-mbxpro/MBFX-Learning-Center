import { describe, expect, it } from "vitest";
import * as theme from "./index";

describe("@repo/theme", () => {
  it("is importable (scaffold smoke test — real coverage lands in Module 02)", () => {
    expect(theme).toBeDefined();
  });
});
