// @repo/contracts — Zod schemas + shared TS types (API contract).
// Use Zod v4 when this fills in (different error APIs than v3 — see A1).
// Implementation lands alongside the modules that need validated I/O,
// starting with Module 05 (see plan.md Part D, docs/logs/DEVLOG.md).
export {};
