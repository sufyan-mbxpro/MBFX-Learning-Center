import { describe, expect, it } from "vitest";
import * as i18n from "./index";

describe("@repo/i18n", () => {
  it("is importable (scaffold smoke test — real coverage lands in Module 06)", () => {
    expect(i18n).toBeDefined();
  });
});
