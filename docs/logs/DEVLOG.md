# DEVLOG

Append-only. Newest entry at the bottom. Every module completion appends:
date, module, what shipped, what was decided/deferred, test status.
`pnpm governance:check` fails a branch that touches `packages/*` without
appending here. Never rewrite or reorder existing entries.

---

## 2026-08-31 — Kickoff (pre-Module 00)

**Context established.** Repo contained only the reviewed planning bundle;
normalized `Docs/` → `docs/` (Windows/Linux casing trap), inputs moved to
`docs/reference/`, plan at `docs/plan.md`.

**Day-1 decisions (registry sweep against live npm/nodejs.org):**

- Prisma 8 still RC → **locked Prisma 7.10.x** (ADR-002).
- `typescript@latest` is now the 7.0.2 native compiler with no stable API →
  **pinned 6.0.3 exactly** (ADR-010); revisit at TS 7.1.
- pnpm 10 → **11.24.0** and Node 22 → **24 LTS**: the plan's own lines had
  moved; recorded in `docs/memory/stack.md`, not separate ADRs (mechanical).

**Architecture revision (owner-directed):** two apps → **one Next.js app**
with `(public)`/`(admin)` route groups and multiple root layouts — the
officially documented Next.js pattern. ADR-006 records the decision, the four
trade-offs, and the mandatory compensating controls (the cookie-scope loss is
real; STAFF gate + server-side re-checks + per-path CSP + blocking Lighthouse
budgets compensate). `docs/plan.md`, `MONOREPO_ARCHITECTURE.md`, and
`MONOREPO_CONFIG.md` revised to match before feature code. Change landed
during Module 00 — nothing was built against the superseded shape.

**Deferred:** ADR-001 (Better Auth — after the Module 04 spike), ADR-003/004/
005/008 (Module 02), ADR-007 (Module 06), ADR-009 (Module 11).

---

## 2026-08-31 — Module 00: Repo scaffold & governance — COMPLETE

**Shipped:**

- Workspace: pnpm 11.24.0 + Turborepo 2.10.12, `apps/* packages/* tooling/*`;
  `onlyBuiltDependencies` (+`unrs-resolver` allowBuilds) and
  `minimumReleaseAge` in `pnpm-workspace.yaml` per pnpm 11.
- `apps/web` via `create-next-app@latest` (Next 16.3.3, TS, Tailwind 4.3,
  ESLint, Turbopack, no src dir), restructured per ADR-006 into `(public)`
  and `(admin)` route groups with separate root layouts (official
  multiple-root-layouts convention), official `error`/`not-found` files per
  surface, root `proxy.ts` (named `proxy` export + matcher; STAFF-gate and
  i18n TODOs pointing at Modules 04/06), generator `AGENTS.md`/`CLAUDE.md`
  kept (regenerated by `next dev`).
- Ten package skeletons (`db auth rbac theme ui i18n core contracts settings
utils`) with lint/typecheck/test scripts, smoke tests, and a stub
  `@repo/db#generate` so the turbo graph is final-shape now.
- `tooling/`: typescript-config (base/react-library/nextjs, strict),
  eslint-config (flat: base/react-internal/next — hex-literal ban,
  physical-property ban, `import-x/no-cycle`), tailwind-config placeholder.
- Governance: `claude.md`, 4 rules files, 15 SKILL.md files, ADR-000/002/
  006/010, `stack.md`, this DEVLOG, `governance:check` +
  `check:phantom-deps` (both with vitest suites incl. the required negative
  test), CI workflow (install → governance → lint → typecheck → test →
  build; remote-cache env stubs).
- `.env.example` with Better Auth naming; SEED_ADMIN_* comment preserved.

**Decisions:** ESLint stays ^9.39.5 (official create-next-app pin;
eslint-config-next's plugin tree caps at ^9) with `eslint-plugin-import-x`
for cycle detection — recorded in stack.md follow-ups. `next typegen` wired
into the app's typecheck (Next 16's generated `LayoutProps`).

**Test status (all green, local):** `lint` 12/12 · `typecheck` 12/12 ·
`test` 12/12 (11 package smoke suites) · `test:governance` 13/13 ·
`check:phantom-deps` OK · `format:check` OK · `build` OK (route table:
`/` static, `/admin` dynamic ƒ, proxy registered) · dev-server smoke:
`/`→200 public layout, `/admin`→200 admin layout + noindex, unknown→404.
Remote-cache hit not yet demonstrable (no TURBO_TOKEN linked) — noted as
the one open DoD item; CI env stubs are in place.

**Next:** Module 01 (`@repo/db`) — waits for go-ahead. Better Auth spike
(ADR-001) can run in parallel per plan Part F.
