all the email should be shift in tabs under one email settings..
---
all the ai settings should also use the tabs in one section & only show in the settings page with page content
----
we will use the anhtropic for the ai..check the usage & stats that is listed will work in real time
curl https://api.anthropic.com/v1/messages \
  --header "x-api-key: $ANTHROPIC_API_KEY" \
  --header "anthropic-version: 2023-06-01" \
  --header "content-type: application/json" \
  --data '{"model": "claude-opus-5", "max_tokens": 1024,
    "messages": [{"role": "user", "content": "Hello, world"}]}'

---
place media after the market data in admin sidebar
---

![alt text](image-6.png)
there should be smart in size vertically..reduce the card hight..
also the text(We email a confirmation link first. Unsubscribe any time — no spam, ever.) should display in single line..
----
Reference