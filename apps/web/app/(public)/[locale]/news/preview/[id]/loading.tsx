// A preview renders the article DETAIL layout, so it takes the detail
// skeleton. Without this file it inherited `news/loading.tsx` — a listing of
// cards standing in for a page of prose (changes-21 Phase A).
export { default } from "../../[slug]/loading.tsx";
